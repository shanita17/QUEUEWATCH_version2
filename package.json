{
  "dependencies": {
    "dotenv": "^17.4.2",
    "nodemailer": "^8.0.8",
    "sqlite": "^5.1.1",
    "sqlite3": "^6.0.1"
  },
  "scripts": {
    "postinstall": "cd server && npm install --build-from-source sqlite3",
    "start": "cd server && node server.js"
  },
  "engines": {
    "node": "20.x"
  }
}
